<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="InvestDao">
	<select id="memberCoin" resultType="MemberCoin">
		select m.id, m.krw, c.coincode, c.coin_amt from memberinfo m inner join coin c on m.id=c.id where c.id='#{id}' and c.coincode='#{coincode}';
	</select>
</mapper>